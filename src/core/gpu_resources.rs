#[derive(Copy, Clone)]
pub struct BufferID {
    pub(crate) id: u64,
}

#[derive(Copy, Clone)]
pub struct ImageID {
    pub(crate) id: u64,
}

#[derive(Copy, Clone)]
pub struct SamplerID {
    pub(crate) id: u64,
}

#[derive(Copy, Clone)]
pub struct ImageViewID {
    pub(crate) id: u64,
}
